---
name: Feedback - YAML Extension
description: YAML ファイルの拡張子は .yaml を使う（.yml ではなく）
type: feedback
---

YAML ファイルの拡張子は `.yaml` を使用する。`.yml` は使わない。

**Why:** ユーザーが明示的に `.yaml` を指定。

**How to apply:** 新規作成・リネーム時は常に `.yaml` 拡張子を使用する。
